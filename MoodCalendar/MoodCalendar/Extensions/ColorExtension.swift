import SwiftUI

extension Color {
    static let customBackground = Color(red: 0.53, green: 0.81, blue: 0.92) // Нежно-синий
    static let customText = Color.white
    static let inactiveTab = Color(white: 0.8, opacity: 0.5)
    
    // Цвета для настроений (как у неактивного таббара)
    static let moodGreat = Color(white: 0.8, opacity: 0.3)
    static let moodHappy = Color(white: 0.8, opacity: 0.25)
    static let moodNeutral = Color(white: 0.8, opacity: 0.2)
    static let moodSad = Color(white: 0.8, opacity: 0.15)
    static let moodTerrible = Color(white: 0.8, opacity: 0.1)
} 